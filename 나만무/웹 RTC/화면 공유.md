


## **📊 Final Comparison Table**

| Feature              | LiveKit            | mediasoup         | Jitsi             |
| -------------------- | ------------------ | ----------------- | ----------------- |
| **Next.js SDK**      | ✅ React components | ⚠️ Low-level only | ⚠️ iFrame or fork |
| **iOS SDK**          | ✅ Native Swift     | ❌ None official   | ✅ Basic           |
| **Code Needed**      | ~50 lines          | ~500+ lines       | ~100 lines        |
| **Development Time** | 1-2 weeks          | 2-3 months        | 2-3 weeks         |
| **Maintenance**      | Easy               | Hard              | Medium            |

## **🎯 Detailed Comparison**

| Factor                | mediasoup         | LiveKit          | Winner                   |
| --------------------- | ----------------- | ---------------- | ------------------------ |
| **Latency**           | ⭐⭐⭐⭐⭐ Ultra-low   | ⭐⭐⭐⭐⭐ Very low   | **TIE** (both excellent) |
| **Video Quality**     | ⭐⭐⭐⭐⭐ Excellent   | ⭐⭐⭐⭐⭐ Excellent  | **TIE**                  |
| **CPU Efficiency**    | ⭐⭐⭐⭐⭐ (500/core)  | ⭐⭐⭐ (100/core)   | **mediasoup** (5x)       |
| **Scalability**       | ⭐⭐⭐⭐⭐ 2x capacity | ⭐⭐⭐⭐ Good        | **mediasoup**            |
| **Stability**         | ⭐⭐⭐⭐⭐ Rock solid  | ⭐⭐⭐⭐⭐ Rock solid | **TIE**                  |
| **Development Time**  | ⭐⭐ 3-5 months     | ⭐⭐⭐⭐ 3-4 weeks   | **LiveKit**              |
| **SDKs Available**    | ⭐⭐ Limited        | ⭐⭐⭐⭐⭐ Excellent  | **LiveKit**              |
| **Documentation**     | ⭐⭐⭐ Technical     | ⭐⭐⭐⭐⭐ Excellent  | **LiveKit**              |
| **Built-in Features** | ⭐⭐ DIY            | ⭐⭐⭐⭐⭐ Complete   | **LiveKit**              |


``` tsx
// app/monitoring/page.tsx
'use client';
import { LiveKitRoom, GridLayout, ParticipantTile } from '@livekit/components-react';
import '@livekit/components-styles';

export default function ControlTower() {
  return (
    <div className="h-screen">
      <h1>담소 모니터링 센터</h1>
      
      <LiveKitRoom
        serverUrl={process.env.NEXT_PUBLIC_LIVEKIT_URL}
        token={token}
      >
        {/* Automatic 7x7 grid for 49 elders */}
        <GridLayout>
          <ParticipantTile />
        </GridLayout>
      </LiveKitRoom>
    </div>
  );
}
```

``` swift
// ElderVideoViewController.swift
import LiveKit
import UIKit

class ElderVideoViewController: UIViewController {
    private let room = Room()
    private var localVideoView: VideoView?
    
    override func viewDidLoad() {
        super.viewDidLoad()
        connectToMonitoring()
    }
    
    func connectToMonitoring() async {
        do {
            // Connect to LiveKit room
            try await room.connect(
                url: "wss://livekit.damso.kr",
                token: elderToken
            )
            
            // Start camera
            try await room.localParticipant.setCamera(enabled: true)
            
            // That's it! Video is streaming
        } catch {
            print("Failed to connect: \(error)")
        }
    }
}
```

``` ts
// backend/src/livekit/livekit.service.ts
import { Injectable } from '@nestjs/common';
import { AccessToken } from 'livekit-server-sdk';

@Injectable()
export class LivekitService {
  generateElderToken(elderId: string, elderName: string): string {
    const token = new AccessToken(
      process.env.LIVEKIT_API_KEY,
      process.env.LIVEKIT_API_SECRET,
      {
        identity: elderId,
        name: elderName,
      }
    );

    token.addGrant({
      room: 'monitoring-room',
      roomJoin: true,
      canPublish: true,
      canSubscribe: true,
    });

    return token.toJwt();
  }
}
```


### **LiveKit 사용하세요!**
```
아이폰 앱 (노인분들)
├─ LiveKit Swift SDK 사용
├─ 카메라만 송출 (10줄 코드)
├─ 자동 재연결
└─ 배터리 최적화 내장

     ↓ (WebRTC 스트림)

LiveKit 서버
├─ 49개 스트림 라우팅
├─ 자동 품질 조절
└─ 녹화 기능 내장

     ↓ (WebRTC 스트림)

Next.js 관제 센터
├─ LiveKit React SDK
├─ 7x7 그리드 자동 생성
└─ AI 분석 오버레이
```