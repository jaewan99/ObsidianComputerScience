---
tags:
  - git
  - convention
  - damso
  - development
  - standards
created: 2024-12-25
project: 담소
status: active
type: guideline
---

# 담소 프로젝트 Git Convention

> [!info] 문서 정보
> **목적**: 팀 전체가 일관된 Git 사용 규칙을 따르기 위함
> **적용 범위**: 모든 커밋, PR, 이슈
> **준수 레벨**: 필수 (Mandatory)

---

## 📝 목차

1. [[#Commit Convention|Commit Convention]]
2. [[#Branch Naming|Branch Naming]]
3. [[#Pull Request Convention|Pull Request Convention]]
4. [[#Issue Convention|Issue Convention]]
5. [[#Code Review Guidelines|Code Review Guidelines]]

---

## 🔖 Commit Convention

### 기본 형식

```
<type>: <subject>

<body>
+ git 메시지 사용
```

### Type (필수)

커밋의 목적을 나타냅니다.

| Type         | 설명                | 예시                              |
| ------------ | ----------------- | ------------------------------- |
| **feat**     | 새로운 기능 추가         | `feat(대시보드): 대상자 목록 필터 추가`      |
| **fix**      | 버그 수정             | `fix(영상통화): 연결 끊김 오류 해결`        |
| **docs**     | 문서 수정             | `docs(README): 설치 가이드 업데이트`     |
| **style**    | 코드 포맷팅 (기능 변경 없음) | `style(components): 코드 포맷팅 적용`  |
| **refactor** | 코드 리팩토링           | `refactor(api): API 호출 로직 개선`   |
| **test**     | 테스트 코드 추가/수정      | `test(dashboard): 필터 기능 테스트 추가` |
| **chore**    | 나머지               | `chore(deps): comment 변경`       |
|              |                   |                                 |


### Subject (필수)

> [!important] Subject 작성 규칙
> - 50자 이내로 작성
> - 명령형으로 작성 ("추가했음" ❌, "추가" ✅)
> - 마침표 없음
> - 한글 또는 영어 사용 (일관성 유지)

**좋은 예:**
```
feat(대시보드): 대상자 검색 기능 추가
fix(영상통화): WebRTC 연결 타임아웃 해결
refactor(api): REST API를 GraphQL로 변경
```

**나쁜 예:**
```
feat: 기능 추가함.  ❌ (마침표, 과거형)
fix bug  ❌ (scope 없음, 설명 부족)
영상통화 수정  ❌ (type 없음)
feat(dashboard): Added search functionality for elderly list  ❌ (과거형)
```

### Body (선택)

> [!tip] Body 작성 시점
> - 무엇을(What) 변경했는지는 Subject에
> - 왜(Why), 어떻게(How)는 Body에 작성

```
feat(대시보드): 대상자 검색 기능 추가

대상자가 많아질 경우를 대비하여 검색 기능을 추가했습니다.
이름, 나이, 지역으로 필터링 가능합니다.

구현 방법:
- Debounce를 적용하여 성능 최적화
- 정규식으로 한글/영어 모두 지원
```

---

## 🌿 Branch Naming

### 형식

```
feature/#3/user_create(간단 키워드)
```

### Type

| Type         | 용도      | 예시                               |
| ------------ | ------- | -------------------------------- |
| **feature**  | 새 기능 개발 | `feature/#3/user_create(간단 키워드)` |
| **fix**      | 버그 수정   | `fix/영상통화-연결-오류`                 |
| **refactor** | 리팩토링    | `refactor/API-구조-개선`             |
| **docs**     | 문서 작업   | `docs/README-업데이트`               |
| **test**     | 테스트 코드  | `test/대시보드-E2E-추가`               |
| **chore**    | 기타 작업   | `chore/의존성-업데이트`                 |


---

## 🔄 Pull Request Convention
### PR 제목 형식

```
[Type (branch type)] issue 제목
```

**예시:**
```
[Feature] 보호자 대시보드 구현
[Fix] 영상 통화 연결 끊김 해결
[Refactor] API 응답 형식 통일
[Docs] 설치 가이드 업데이트
```

### PR 템플릿
````markdown
 📋 변경 사항

- 이 PR에서 변경한 내용을 간단히 설명해주세요.
  - 세부 변경 사항
- 추가 변경
- 추가 변경 2

 🔗 관련 이슈

Closes #이슈번호
````

### PR 작성 예시

````markdown
 📋 변경 사항

- 보호자 대시보드에 대상자 목록 필터링 기능을 추가했습니다.

 🔗 관련 이슈

Closes #42
````

### PR 크기 가이드라인

> [!tip] 적절한 PR 크기
> - **작은 PR** (권장): 200줄 이하
> - **중간 PR**: 200-500줄
> - **큰 PR** (지양): 500줄 이상
>
> 큰 PR은 여러 개로 쪼개는 것을 "권장"합니다.  코드 길이가 길어지면 branch 쪼개기.

### PR 라벨 (나중 논의)

GitHub Labels를 활용하여 PR을 분류합니다.

| Label           | 용도    | 색상            |
| --------------- | ----- | ------------- |
| `feature`       | 새 기능  | 🟢 Green      |
| `bug`           | 버그 수정 | 🔴 Red        |
| `refactor`      | 리팩토링  | 🔵 Blue       |
| `documentation` | 문서    | 📘 Light Blue |
| `urgent`        | 긴급    | 🟠 Orange     |
| `WIP`           | 작업 중  | 🟡 Yellow     |
| `review-needed` | 리뷰 요청 | 🟣 Purple     |

---

## 🐛 Issue Convention

### Issue 제목 형식

```
[Type] 간단한 설명
```

**예시:**
```
[Bug] 영상 통화 연결이 끊기는 문제
[Feature] 대상자 검색 기능 추가 요청
[Question] AWS Lambda 설정 관련 질문
[Enhancement] 대시보드 로딩 속도 개선
```

### Issue label

| type              | 설명      | 라벨 색상         |
| ----------------- | ------- | ------------- |
| **Bug**           | 버그 리포트  | 🔴 Red        |
| **Feature**       | 새 기능 제안 | 🟢 Green      |
| **Enhancement**   | 기능 개선   | 🔵 Blue       |
| **Documentation** | 문서 관련   | 📘 Light Blue |
| **Performance**   | 성능 이슈   | 🟠 Orange     |

### Issue 템플릿

````markdown
필요한거
````

### Feature Request 템플릿

````markdown
 ✨ 기능 설명

제안하는 기능에 대해 명확하고 간결하게 설명해주세요.

 📋 상세 내용
- 내용
````

### Feature Request 작성 예시

````markdown
 ✨ 기능 설명

보호자 대시보드에 대상자별 통화 히스토리를 차트로 시각화하는 기능

 📋 상세 내용

**기능 요구사항:**
1. 통화 빈도 차트 (주간/월간)

````

### Issue 라벨 시스템

#### Priority (우선순위)

| Label | 의미 | 대응 시간 |
|-------|------|-----------|
| `priority: critical` | 시스템 다운 | 즉시 |
| `priority: high` | 주요 기능 영향 | 1일 이내 |
| `priority: medium` | 일반 개선 | 1주 이내 |
| `priority: low` | 편의 기능 | 추후 검토 |

#### Status (상태)

| Label | 의미 |
|-------|------|
| `status: open` | 새로운 이슈 |
| `status: in-progress` | 작업 중 |
| `status: blocked` | 블로킹됨 |
| `status: review` | 리뷰 대기 |
| `status: closed` | 완료 |

#### Component (컴포넌트)

| Label | 범위 |
|-------|------|
| `component: frontend` | 프론트엔드 |
| `component: backend` | 백엔드 |
| `component: ai` | AI/ML |
| `component: infrastructure` | 인프라 |
| `component: database` | 데이터베이스 |

---

## 👥 Code Review Guidelines

### 리뷰어 역할

> [!important] 리뷰어의 책임
> - 코드 품질 검증
> - 버그 발견
> - 베스트 프랙티스 제안
> - 지식 공유

### 리뷰 체크리스트

```markdown
 기능 동작
- [ ] 요구사항을 충족하는가?
- [ ] 엣지 케이스를 고려했는가?
- [ ] 에러 핸들링이 적절한가?

 코드 품질
- [ ] 코드가 이해하기 쉬운가?
- [ ] 중복 코드가 없는가?
- [ ] 함수/변수명이 명확한가?
- [ ] 주석이 필요한 부분에 작성되었는가?

 테스트
- [ ] 유닛 테스트가 작성되었는가?
- [ ] 테스트 커버리지가 충분한가?
- [ ] 모든 테스트가 통과하는가?

 성능
- [ ] 성능 이슈가 없는가?
- [ ] 불필요한 API 호출이 없는가?
- [ ] 메모리 누수 가능성이 없는가?

 보안
- [ ] 보안 취약점이 없는가?
- [ ] 민감 정보가 노출되지 않는가?
- [ ] 입력 검증이 적절한가?

 문서화
- [ ] README 업데이트가 필요한가?
- [ ] API 문서가 최신인가?
- [ ] 주석이 충분한가?
```

### 리뷰 코멘트 작성 가이드

#### 좋은 리뷰 코멘트

> [!success] 건설적인 피드백
> 
> **문제 지적 + 해결 제안:**
> ```
> 이 부분에서 N+1 쿼리 문제가 발생할 수 있습니다.
> 
> 다음과 같이 eager loading을 사용하는 것은 어떨까요?
> 
> `User.query.includes(:profile).all`
> ```

> [!success] 칭찬과 학습
> ```
> 이 에러 핸들링 방식 좋네요! 저도 배워갑니다 👍
> ```

#### 나쁜 리뷰 코멘트

> [!danger] 피해야 할 코멘트
> 
> **비난조:**
> ```
> ❌ "이게 뭐예요? 이해가 안 되는데요?"
> ```
> 
> **해결책 없는 지적:**
> ```
> ❌ "이 코드 별로인 것 같아요."
> ```
> 
> **일방적 강요:**
> ```
> ❌ "무조건 이렇게 바꾸세요."
> ```

### 리뷰 응답 가이드

#### PR 작성자의 태도

> [!tip] 피드백 받을 때
> - 🙏 감사하게 받아들이기
> - 🤔 이해 안 되면 질문하기
> - 💬 반대 의견도 정중하게 설명
> - ✅ 수정 후 리뷰어에게 알리기

**좋은 응답:**
```
좋은 지적 감사합니다! 
말씀하신 대로 수정했습니다.

커밋: abc123
```

**질문이 있을 때:**
```
좋은 제안이네요! 
다만 eager loading을 사용하면 메모리 사용량이 
증가할 수 있어서 고민됩니다.

대상자가 1000명 이상일 때도 괜찮을까요?
```

### 리뷰 승인 기준

#### Approve 시점

```
✅ 모든 체크리스트 통과
✅ 중요한 피드백 모두 반영
✅ CI 테스트 통과
✅ 코드 품질 기준 충족
```

#### Request Changes 시점

```
⚠️ 버그 발견
⚠️ 보안 이슈
⚠️ 성능 문제
⚠️ 중요한 테스트 누락
```

#### Comment Only 시점

```
💬 사소한 개선 제안
💬 대안 제시
💬 칭찬 및 격려
💬 추후 개선 사항
```

---

## 📊 Convention 준수 체크리스트

### 커밋 전

```markdown
- [ ] 커밋 메시지가 형식에 맞는가?
- [ ] Type이 적절한가?
- [ ] Subject가 명령형인가?
- [ ] 50자를 넘지 않는가?
```

### PR 생성 전

```markdown
- [ ] PR 제목이 형식에 맞는가?
- [ ] PR 템플릿을 모두 작성했는가?
- [ ] 관련 이슈를 링크했는가?
- [ ] 라벨을 추가했는가?
- [ ] Self-review를 완료했는가?
```

### 이슈 생성 전

```markdown
- [ ] 제목이 명확한가?
- [ ] 템플릿을 사용했는가?
- [ ] 재현 방법이 명확한가? (버그의 경우)
- [ ] 우선순위를 지정했는가?
- [ ] 라벨을 추가했는가?
```

---

## 🎯 실전 예시 모음

### 시나리오 1: 새 기능 개발

```bash
# 1. 브랜치 생성
git checkout -b feature/대상자-검색

# 2. 개발 및 커밋
git add .
git commit -m "feat(대시보드): 대상자 이름 검색 기능 추가

debounce 300ms 적용으로 성능 최적화

Closes #42"

# 3. 추가 개발
git commit -m "feat(대시보드): 나이 필터 기능 추가"
git commit -m "test(대시보드): 검색 기능 유닛 테스트 추가"

# 4. Push 및 PR
git push origin feature/대상자-검색
```

**GitHub PR:**
```
제목: [Feature] 대상자 검색 및 필터 기능

설명: (템플릿 참고)

라벨: feature, dashboard
Assignee: 김상연
Reviewer: 문성수, 배재완
```

### 시나리오 2: 버그 수정

```bash
# 1. 이슈 생성
GitHub Issue #89
제목: [Bug] 영상 통화 5분 후 자동 종료됨

# 2. 브랜치 생성
git checkout -b fix/video-timeout

# 3. 수정 및 커밋
git commit -m "fix(영상통화): WebRTC 타임아웃 30분으로 증가

기본값 5분에서 30분으로 변경
inactivity timeout도 함께 조정

Fixes #89"

# 4. 테스트 추가
git commit -m "test(영상통화): 장시간 통화 테스트 추가"

# 5. PR 생성
```

**GitHub PR:**
```
제목: [Fix] 영상 통화 타임아웃 이슈 해결

라벨: bug, urgent, video
Closes #89
```

### 시나리오 3: 리팩토링

```bash
# 1. 브랜치 생성
git checkout -b refactor/api-error-handling

# 2. 리팩토링 커밋
git commit -m "refactor(api): 에러 핸들링 통일

- 모든 API에 try-catch 적용
- 커스텀 에러 클래스 도입
- 에러 로깅 표준화"

git commit -m "refactor(api): API 응답 형식 통일

성공/실패 모두 { success, data, error } 형식"

# 3. PR 생성
```

---

## 🛠️ 도구 및 자동화

### Git Hooks (Husky)

커밋 메시지 자동 검증:

```json
// package.json
{
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
```

```javascript
// commitlint.config.js
module.exports = {
  extends: ['@commitlint/config-conventional'],
  rules: {
    'type-enum': [
      2,
      'always',
      ['feat', 'fix', 'docs', 'style', 'refactor', 'test', 'chore', 'perf']
    ],
    'subject-max-length': [2, 'always', 50],
    'subject-case': [0]  // 한글 허용
  }
};
```

### GitHub Actions

PR 자동 체크:

```yaml
# .github/workflows/pr-check.yml
name: PR Convention Check

on:
  pull_request:
    types: [opened, edited]

jobs:
  check-title:
    runs-on: ubuntu-latest
    steps:
      - name: Check PR Title
        run: |
          if [[ ! "${{ github.event.pull_request.title }}" =~ ^\[(Feature|Fix|Refactor|Docs|Test|Chore)\] ]]; then
            echo "PR 제목이 형식에 맞지 않습니다"
            exit 1
          fi
```

---

## 📖 참고 자료

- [Conventional Commits](https://www.conventionalcommits.org/)
- [Semantic Versioning](https://semver.org/)
- [Keep a Changelog](https://keepachangelog.com/)
- [How to Write a Git Commit Message](https://chris.beams.io/posts/git-commit/)

---

## 📎 관련 문서

- [[담소_Git_Branch_전략]]
- [[코드_리뷰_가이드]]
- [[코딩_컨벤션]]

---

**마지막 업데이트**: 2024-12-25
**작성자**: 담소 개발팀
**버전**: 1.0
**준수 레벨**: Mandatory (필수)
